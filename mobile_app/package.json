{
  "name": "perimeter-mobile",
  "version": "1.0.0",
  "description": "Mobile wrapper for Perimeter dashboard",
  "main": "index.html",
  "scripts": {
    "start": "npx cap sync",
    "build:android": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
